CREATE TABLE `RESERVATION` (
  `NO` INT NOT NULL AUTO_INCREMENT,
  `NAME` VARCHAR(45) NOT NULL,
  `DATE` VARCHAR(30) NOT NULL,
  `TIME` VARCHAR(10) NOT NULL,
  `PERSONNEL` VARCHAR(20) NULL,
  `PHONE` VARCHAR(30) NOT NULL,
  `EMAIL` VARCHAR(100) NULL,
  `NOTICE` VARCHAR(300) NULL,
  `PASSWORD` VARCHAR(100) NOT NULL,
  PRIMARY KEY (`NO`),
  UNIQUE INDEX `NO_UNIQUE` (`NO` ASC))
ENGINE = InnoDB;

SELECT
	  NO
	 ,NAME 
	 ,DATE 
	 ,TIME
	 ,PERSONNEL
	 ,PHONE
	 ,EMAIL
	 ,NOTICE
	 ,PASSWORD 
FROM  RESERVATION
WHERE 1=1;

DROP TABLE RESERVATION;

INSERT INTO RESERVATION (
	  NAME 
	 ,DATE 
	 ,TIME
	 ,PERSONNEL
	 ,PHONE
	 ,EMAIL
	 ,NOTICE
	 ,PASSWORD
)
VALUES
(
 	  'John'
     ,'John'
     ,'John'
     ,'John'
     ,'John'
     ,'John'
     ,'John'
     ,'John'
);

DELETE FROM RESERVATION WHERE NO = 2;

UPDATE RESERVATION SET 
	  NAME = 'Jo'
	 ,DATE = 'Jo' 
	 ,TIME = 'Jo'
	 ,PERSONNEL = 'Jo'
	 ,PHONE = 'Jo'
	 ,EMAIL = 'Jo'
	 ,NOTICE = 'Jo'
	 ,PASSWORD = 'Jo'
WHERE 1=1
AND   NO = 3;

SELECT * FROM RESERVATION;









